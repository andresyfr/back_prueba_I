package com.andresyfr.biblioteca.personalizados;

import static org.junit.jupiter.api.Assertions.*;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.autoconfigure.web.servlet.AutoConfigureMockMvc;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.boot.test.context.SpringBootTest.WebEnvironment;

@AutoConfigureMockMvc
@SpringBootTest(webEnvironment = WebEnvironment.RANDOM_PORT)
class TestJunitFive {

	/**
	 * Se puede mejorar la estructura de los test, para que implemente junitJpiter
	 * esto cambiaria en algo la estructura de la clase prestamo Tests y el build.gradle
	 * por temas de tiempo y cómo el archivo se encuentra en modo lectura queda en standby (Y)
	 */
}
